# Пчелошеринг

Сайт каталога экзотических растений и пчелошеринга. Статика: HTML, CSS, JS.

Каталог растений — стартовая страница. Улей: `hive.html`.

Фото растений лучше сжимать в WebP (~100 КБ), иначе GitHub Pages быстро упрётся в лимит размера. Скрипт: `python3 scripts/compress_photos.py`.
